# Créditos — assets da Fase 4

Nenhum arquivo desta pasta vem de banco de imagem, foto de terceiro ou API de geração.
Todos os oito são **gerados por código**, desenhados como SVG nesta sessão da esteira
(papel Diretor de mídia, skill `imagem-por-codigo`), usando só os tokens de
`docs/esteira/fase-4-acesso-windows-e-pwa-android/design.md`.

| Arquivo | Origem | Autor | Licença |
|---|---|---|---|
| `favicon.svg` | Gerado por código (SVG à mão, sem biblioteca) | Diretor de mídia, esteira Fase 4 | Proprietária — mesma licença do repositório `cora-med` |
| `icon-any.svg` | Gerado por código | idem | idem |
| `icon-maskable.svg` | Gerado por código | idem | idem |
| `icon-windows-source.svg` | Gerado por código | idem | idem |
| `wordmark-lockup.svg` | Gerado por código (tipografia em `<text>`, fonte Inter já adotada pelo projeto) | idem | idem |
| `wordmark-only.svg` | Gerado por código | idem | idem |
| `ilustracao-vazio.svg` | Gerado por código | idem | idem |
| `ilustracao-erro-conexao.svg` | Gerado por código | idem | idem |

**Fonte usada nos wordmarks:** Inter, já escolhida em `design.md` (`--font-sans`), licença
SIL Open Font License 1.1 — gratuita para uso comercial, auto-hospedável. Não é este pacote
que a baixa; a tela (`apps/web`, ainda não escrita) carrega a mesma Inter já decidida.

**Nunca hotlink, nunca API paga de geração de imagem** — nenhuma das duas foi usada.
